spring:
  datasource:
    url: jdbc:h2:mem:ejemplo
    driver-class-name: org.h2.Driver
    data-username: sa
    password:
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: create
  h2:
    console:
      enabled: true
